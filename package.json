{
  "name": "newstackoverflow",
  "version": "1.0.0",
  "description": "replacement for stack overflow",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrent": "^0.3.2",
    "concurrently": "^5.1.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.9",
    "nodemon": "^2.0.3",
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "react-scripts": "^3.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "cd que_api && node app.js",
    "dev": "cd que_client && npm start",
    "test": "cd que_client && npm test",
    "preinstall": "cd que_api && npm install",
    "install": "cd que_client && npm install",
    "build": "cd que_client && npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}